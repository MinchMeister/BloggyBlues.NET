@model IEnumerable<BloggyBlues.Models.Post>

@{
    ViewBag.Title = "Index";
}

<div class="text-center">
    <div>
        <input type="text" ng-model="models.Bob" />
        <h1>{{models.Bob}}</h1>
        <button ng-click="offWork()">Clock out</button>
    </div>
    @Html.ActionLink("Create New", "Create", null, htmlAttributes: new { id="createButton", @class="button tiny"})
    <br />
    <br />
</div>

@Html.Partial("_PostPartial")